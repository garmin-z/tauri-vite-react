// use std::{
//     process::Child,
//     sync::{Arc, Mutex},
// };

// pub struct AppState {
//     go_process: Option<Child>,
// }

// // pub static APP_STATE: Arc<Mutex<AppState>> = Arc::new(Mutex::new(AppState { go_process: None }));
